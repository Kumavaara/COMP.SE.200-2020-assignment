language: node_js
node_js:
  - 'stable'

install:
  - npm i

script: 
  - npm run test -- --coverage

after_script:
  - npm run coveralls

deploy:
  provider: s3
  access_key_id: AWSAccessKeyId
  secret_access_key: AWSSecretAccessKey
  bucket: "S3 Bucket"
  skip_cleanup: true